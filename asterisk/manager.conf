; ═══════════════════════════════════════════════════════════════════════
; AMI (Asterisk Manager Interface) Configuration
; ═══════════════════════════════════════════════════════════════════════

[general]
enabled = yes
port = 5038
bindaddr = 0.0.0.0

; AMI user for Centro de Control backend
[centro_ami]
secret = centro_ami_secret
deny = 0.0.0.0/0.0.0.0
permit = 0.0.0.0/0.0.0.0
read = system,call,log,agent,user,config,dtmf,reporting,cdr,dialplan
write = system,call,agent,user,config,command,reporting,originate
writetimeout = 5000
